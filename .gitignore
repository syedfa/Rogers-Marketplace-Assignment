# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
*.moved-aside
*.xcarchive
.build/

# Swift Package Manager
.swiftpm/

# Mock server: reproducible, generated by scripts/setup.sh — not committed
mock-server/db.json
mock-server/public/images/

# Node (in case anything ever runs `npm install` locally against mock-server)
node_modules/
npm-debug.log*

# Test results
*.xcresult

# Local-only notes, not meant to be pushed
docs/WALKTHROUGH.md
